server:
  port: 8762
spring:
  application:
    name: native_configserver
  profiles:
    active: native #配置文件的获取方式--本地
  cloud:
    config:
      server:
        native:
          search-locations: classpath:shared/ #本地配置文件存放路径